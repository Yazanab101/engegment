services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: engegment
      POSTGRES_PASSWORD: engegment
      POSTGRES_DB: engegment
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
